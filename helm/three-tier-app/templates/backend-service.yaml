apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: {{ .Values.backendService.port }}
      targetPort: 5000
  type: {{ .Values.backendService.type }}
